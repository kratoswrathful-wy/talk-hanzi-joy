---
description: XLIFF / mqxliff / sdlxliff tag 系統規則。凡涉及 xliff-tag-pipeline.js、xliff-import.js、app.js 中 tag 顯示或匯出的修改，必須遵守本規則。
globs:
  - "cat-tool/app.js"
  - "cat-tool/js/xliff-tag-pipeline.js"
  - "cat-tool/js/xliff-import.js"
  - "public/cat/app.js"
  - "public/cat/js/xliff-tag-pipeline.js"
  - "public/cat/js/xliff-import.js"
  - "src/lib/cat-cloud-rpc.ts"
alwaysApply: false
---

# XLIFF Tag Export & Display 規則

完整研究記錄請查閱 `docs/XLIFF_TAG_PIPELINE.md`。以下是修改時必須遵守的強制規則。

## 禁止事項

### 1. 禁止用 `textContent` 更新 `.grid-textarea`

```javascript
// 錯誤 — textContent 會清除所有 pill span
ta.textContent = seg.targetText;

// 正確
ta.innerHTML = buildTaggedHtml(seg.targetText || '', effectiveTags(seg));
updateTagColors(row, seg.targetText || '');
```

### 2. 禁止用 `||` fallback 空陣列 tags

```javascript
// 錯誤 — 空陣列 [] 是 truthy，|| 不會 fallback
buildTaggedHtml(text, seg.targetTags || seg.sourceTags || [])

// 正確 — 用 effectiveTags helper
buildTaggedHtml(text, effectiveTags(seg))
```

`effectiveTags` 定義在 `app.js`，緊鄰 `buildTaggedHtml`：
```javascript
function effectiveTags(seg) {
    return (seg.targetTags && seg.targetTags.length > 0)
        ? seg.targetTags
        : (seg.sourceTags || []);
}
```

### 3. 禁止移除 `transparentG` 參數傳遞

sdlxliff 的 mrk 內部擷取**必須**傳 `{ transparentG: false }`：
```javascript
Xliff.extractTaggedText(srcMrk, { transparentG: false })
```
整個 `<source>` / `<target>` 的 sdlxliff 匯入用 `{ transparentG: true }`。
兩者不可混用，否則會產生多餘或缺失的 tag 佔位符。

### 4. 禁止直接把 targetText 寫入 XML

匯出時必須先還原 tag：
```javascript
// 正確流程
const tags = seg.targetTags && seg.targetTags.length ? seg.targetTags : (seg.sourceTags || []);
const restoredXml = replacePlaceholders(targetText, tags, seg.sourceTags || []);
setXmlTargetContent(xmlDoc, targetNode, restoredXml, { tuId });
```

### 5. 禁止隨意修改 `bptMap` 的作用域

`bptMap` 必須在每次 `extractTaggedText` 呼叫時重新建立（函式內部局部變數），
不可改為全域或跨句段共用。

### extractTaggedText：`display`（pill 文字）解析順序

僅影響 UI，**不影響** `xml` 與匯出。`ph` / `it` / `x` / `bpt` / `ept` 必須遵守：

1. 有 `displaytext`（memoQ）→ 使用；值為 `{}` 時改走 ctype/type。
2. 否則有 `equiv-text`（XLIFF / Trados，如 Picture）→ 使用。
3. 否則 `textContent`；若為 `{}` 或 `{0}`（memoQ 內部佔位）→ 改走 ctype/type。
4. 仍無則 fallback 編號字串（如 `{N}` 或 `<N>`）。

**禁止**在未確認 memoQ `displaytext="{0}"` 語意前，全域略過字面 `{0}`（會破壞 §4.1 行為）。

## sdlxliff 匯出必須透過 mrk

sdlxliff 匯出**不可**直接替換整個 `<target>` 內容，必須：
1. 找到 `<target>` 下的 `<mrk mtype="seg">`
2. 清空 mrk 內容
3. 用 `setXmlTargetContent(xmlDoc, mrk, restoredXml)` 填入
4. 保留外層 `<g>` 結構不動

對應函式：`_updateSdlxliffMrkContent()`。

## 修改 `extractTaggedText` 前必須測試三種格式

- mqxliff：`<ph>` / `<bpt>` / `<ept>` 元素，displaytext="{0}" 格式
- sdlxliff：`<mrk mtype="seg">` 內的 `<ph>` / `<g>`
- 一般 XLIFF：`<ph>` 元素

### 6. 禁止從 `cat-cloud-rpc.ts` 移除 `source_tags` / `target_tags` 映射

Team 模式下，tags 透過 cloud RPC 存入 Supabase。以下兩處缺一不可：

```typescript
// db.addSegments — 寫入
source_tags: Array.isArray(s.sourceTags) ? s.sourceTags : [],
target_tags: Array.isArray(s.targetTags) ? s.targetTags : [],

// mapSegmentRow — 讀回
sourceTags: tryParseJson(r.source_tags, []),
targetTags: tryParseJson(r.target_tags, []),
```

`segmentExtraCamelToSnake` 也必須保留 `targetTags` → `target_tags` 的映射，
否則 `updateSegmentTarget` 呼叫時 targetTags 更新會被靜默丟棄。

## 參考

- 完整架構：`docs/XLIFF_TAG_PIPELINE.md`
- Pipeline 模組：`cat-tool/js/xliff-tag-pipeline.js`（標頭有 CRITICAL 警告）
- 匯入模組：`cat-tool/js/xliff-import.js`
- Team 模式 RPC：`src/lib/cat-cloud-rpc.ts`
