---
description: 架構與模組化約束。新增程式碼（stores、table-views、工具列按鈕、列表查詢、檔案行數、cat-tool 模組）一律遵守；既有巨檔不因此重構。
alwaysApply: true
---

# 架構規則（強制）

本規則約束**新增**程式碼；既有巨檔（`cat-tool/app.js`、巨型 `.tsx` 頁面）不因此規則被動重構。背景與工項編號見 [`docs/ENGINEERING_IMPROVEMENT_MASTER_PLAN_2026-07.md`](../../docs/ENGINEERING_IMPROVEMENT_MASTER_PLAN_2026-07.md)。

## 1. `cat-tool/app.js` 凍結：只出不進（W7）

- **禁止**在 `cat-tool/app.js` 新增功能。新功能一律建立 `cat-tool/js/<feature>.js` 模組（IIFE/namespace 掛 `window`，比照 `xliff-tag-pipeline.js`），並於 `index.html` 加 `<script>`。
- 修改既有功能時，若改動超過該功能一半，應順勢將該功能整段搬遷至 `js/` 模組。**搬遷 commit 與行為變更 commit 分開**（搬遷 commit 行為零變更）。
- 改完執行 `npm run sync:cat`，`cat-tool` 與 `public/cat` 一併提交。
- **附註（2026-07-04，W9-A）**：句段列狀態刷新的程式碼路徑必呼叫 `syncRowStatusDataset(row, seg)` 同步 `data-status`／`data-wf-state`（供 AI／Playwright 程式化定位，見主計畫 §10 W9-A5）。目前掛於列建立（`buildGridDataRow`）與三個直接操作 `.col-status` 的刷新點（`refreshStatusIconForRow`、`refreshUserMarkerStatusCell`、批次確認刷新迴圈）。**日後在 `app.js` 新增或搬遷狀態刷新路徑時，須同步呼叫此函式**，不得只更新圖示 HTML 而漏掉 dataset。

## 2. `src/stores/` 一律用工廠（W1）

- 新資料實體一律使用 `createEntityStore` 工廠（見 `src/stores/entity-store-factory.ts`）。
- W1 完成前，暫比照 `fee-store` 模式，但**須包含** case-store 的 in-flight optimistic 保護（防 realtime 覆寫未落地的本地寫入）。

## 3. 表格檢視一律用泛用版（W2）

- 一律使用泛用 `use-table-views`（`src/hooks/use-table-views.ts`）；實體差異以參數傳入。
- **禁止**再新增 `use-<entity>-table-views` 複製檔；泛用版做不到的行為先擴充泛用版。

## 4. 工具列按鈕走註冊制

- 工具列按鈕一律走 `ui-button-registry`／`module-toolbar-buttons` 註冊，**禁止**在頁面元件內手刻。

## 5. 列表查詢禁止 `select("*")`（W4）

- 列表查詢必須指定欄位清單常數（比照 `cat-cloud-rpc.ts` 的 `CAT_FILE_LIST_COLUMNS`，命名 `<ENTITY>_LIST_COLUMNS`）。
- 詳情頁再抓單筆全欄位。

## 6. 單檔行數警戒（W8）

- 新建 `.tsx` 超過 800 行、單一元件超過 400 行時，應先拆分再繼續。
- 既有巨檔不因此重構，僅約束新增程式碼；巨型頁面碰到才拆（抽成 `src/components/<area>/<Section>.tsx`，拆分 commit 與功能 commit 分開，禁止全頁重寫）。

## 7. 分支與資料庫同步（強制）

背景：Fable 5 覆核（2026-07-03）發現工程改善 commit 曾與不相關的 CAT 捲動修正混在同一分支，且已套用 DB 的 migration 檔一度只存在分支未進 `main`，造成 repo 與正式資料庫脫鉤。

- **一工項一分支**：每個工項（或一組緊密相關的變更）從**最新 `main`** 切出獨立分支；**禁止**在同一 feature 分支堆疊不相關工項。
- **DB 與版控不得脫鉤**：任何 `supabase db push`（或等效的遠端 DB 變更）所對應的 `supabase/migrations/*.sql`，**必須**與該次變更在**同一分支**、隨同一次 merge 進 `main`。禁止「DB 已套用但 migration 檔還留在別的分支」。
- **migration 需 idempotent**：一律 `CREATE ... IF NOT EXISTS`／`DROP ... IF EXISTS` 後重建，確保檔案併入 `main` 後對已套用的 DB 重跑不出錯。
- **合併前檢查**：merge 進 `main` 前確認該分支只含本工項相關檔案；跨工項的變更應拆分或分別 merge。

## 8. 禁止新增「檔案級 `eslint-disable`」（2026-07-06 裁決）

背景：`src/lib/cat-cloud-rpc.ts` 第 1 行 `/* eslint-disable @typescript-eslint/no-explicit-any */` 掩蓋了該檔約 295 處 `any`，直到 lint 批次 5 盤點才現形（初盤紀錄見 [`docs/ENGINEERING_IMPROVEMENT_MASTER_PLAN_2026-07.md`](../../docs/ENGINEERING_IMPROVEMENT_MASTER_PLAN_2026-07.md) §15）。檔案級 disable 會讓 lint 統計與 CI 完全看不到檔內新增的違規，風險遠高於行內 disable。

- **禁止**在任何 `.ts`／`.tsx` 檔案**新增**檔案級 `eslint-disable`（放在檔案第一行、對整檔生效的形式）。`cat-cloud-rpc.ts` 現存的一處為**唯一既存特例**（清理排程見主計畫 §15，建議與階段四 W1 store 工廠協同），**不得再增加第二個**。
- 需要放寬單一規則時，一律用**行內** `// eslint-disable-line <rule>` 或 `// eslint-disable-next-line <rule>`，且註明理由；行內 disable 仍受 [`testing.mdc`](testing.mdc) 與批次驗收流程的「新增 diff 四項禁用手法 grep」把關，能在 code review／lint 報表中逐行看見，不會像檔案級一樣整批隱形。
- 更好的做法（優先於行內 disable）：改用 `unknown` + 縮小型別、防禦性 mapper 函式（`xxxFromJson`）、或補齊 `types.ts` 型別，參考 lint 批次 5（主計畫 §14）的實際修法案例。

## 9. 本機 `.env` 保護（強制，2026-07-06 裁決）

背景：專案擁有者手動填入的 `PLAYWRIGHT_TEST_EMAIL`／`PLAYWRIGHT_TEST_PASSWORD`（Playwright 端對端測試用的登入帳密）在本機 `.env` 中反覆被清空、需重填多次。根因盤點發現 `.env` 曾被誤加入 Git 版控（已於本次修正移除追蹤，`git rm --cached .env`），任何 `git checkout`／`reset`／流程還原都會把本機新增的行連帶清掉；此外也存在代理直接用「整檔寫入」工具改 `.env` 的風險。

- **禁止整檔覆寫使用者本機 `.env`（或 `.env.playwright.local` 等本機專用 env 檔）**。只能用外科式編輯（讀取後對特定行做增刪，例如 `StrReplace`／等效的局部編輯工具）**追加缺少的鍵**，**絕不刪除或覆寫既有鍵**——尤其 `PLAYWRIGHT_TEST_EMAIL`／`PLAYWRIGHT_TEST_PASSWORD` 等憑證。
- 需要新增環境變數時，**先讀取現有 `.env`**，只 append 缺的行；已存在的鍵不得改值，除非使用者明確要求變更該鍵。
- **任何「重新產生 `.env`」的腳本或動作一律禁止**（例如用範本整檔覆寫、`.env.example` 直接複製蓋過既有 `.env`）。
- **`.env` 不得再加入 Git 版控**（`git add -f .env` 或等效強制加入一律禁止）；`.gitignore` 已排除 `.env`／`.env.*`，僅 `.env.example` 例外。若發現 `.env` 又被追蹤，立即 `git rm --cached .env` 並確認 `git check-ignore .env` 命中。
- **Playwright 本機測試憑證優先放 `.env.playwright.local`**（與一般設定用的 `.env` 分開，降低被誤蓋機率；`playwright.config.ts` 的 `loadDotEnv` 會自動一併讀取，`.env.playwright.local` 優先）。

### `.env.example` 守則

- `.env.example` 進版控、對外公開，**只能放欄位名與假佔位值**（如 `your@email`、`sk-...`、`your_password`），**絕不填真實帳密／金鑰**。
- 真值只進本機 `.env`／`.env.playwright.local`（皆已 `.gitignore` 排除）或 GitHub Secrets／Vercel Project Settings。
- 驗收方式：`git check-ignore .env` 與 `git check-ignore .env.playwright.local` 應命中（輸出對應規則），`git check-ignore .env.example` 不應命中（無輸出、exit code 1）。
