# 客戶請款圖示與狀態標籤修正（最終確認用計畫）

## 已決定範圍
1. **四個 Add 按鈕 icon 修正**：`translator_invoices_add`、`client_invoices_add`、`fees_add`、`internal_notes_add`。
2. **Client Invoices 清單 + 明細 status label 修正**：確保狀態 badge / 篩選顯示使用 `statusLabel` option ids
   - `sl-invoice-pending`
   - `sl-invoice-partial`
   - `sl-invoice-paid`

## 檔案引用（變更責任點）
### Icon fix（四個 Add 按鈕）
- `src/lib/ui-button-registry.ts`：定義上述四個按鈕的 `defaultIconKey` 與 id。
- `src/lib/ui-button-icon-render.tsx`：`UiToolbarButtonIcon` 依 `uiButtonId` + `iconKey/defaultIconKey` 解析並渲染。
- `src/pages/InvoicesPage.tsx`：`translator_invoices_add` 的 Add 按鈕目前使用硬寫 `<Plus />`。
- `src/pages/ClientInvoicesPage.tsx`：`client_invoices_add` 的 Add 按鈕目前使用硬寫 `<Plus />`。
- `src/pages/TranslatorFees.tsx`：`fees_add` 的 Add 按鈕目前使用硬寫 `<Plus />`。
- `src/pages/InternalNotesPage.tsx`：`internal_notes_add` 的 Add 按鈕目前使用硬寫 `<Plus />`。

### Status label fix（Client Invoices list + detail）
- `src/pages/ClientInvoicesPage.tsx`
  - `StatusBadge`：將內部狀態映射至 `statusLabel` option ids（`sl-invoice-*`）取得 `label/bgColor/textColor`。
  - `statusOptionsList`：提供給 `FilterSortToolbar` 的狀態顯示文案（同樣使用 `sl-invoice-*` 對應到 `statusLabel` options 的 label）。
  - `FilterSortToolbar`：當 `filter.field === "status"` 時，會使用 `statusOptionsList` 當作顯示選項。
- `src/pages/ClientInvoiceDetailPage.tsx`
  - `StatusBadge`：同樣依 `sl-invoice-*` option ids 取得顏色/文案。
- `src/stores/select-options-store.ts`：預設 `statusLabel` options 中包含 `sl-invoice-pending/sl-invoice-partial/sl-invoice-paid`。
- `src/components/fees/FilterSortToolbar.tsx`：針對 `filter.field === "status"` 會使用 `statusOptionsList`。

## TODO（精簡）
1. **Icon fix：四個 Add 按鈕改用 `UiToolbarButtonIcon`**
   - 對下列頁面中的「Add」按鈕，把硬寫 `<Plus />` 改成 `<UiToolbarButtonIcon uiButtonId="..." />`：
     - `src/pages/InvoicesPage.tsx`：`translator_invoices_add`
     - `src/pages/ClientInvoicesPage.tsx`：`client_invoices_add`
     - `src/pages/TranslatorFees.tsx`：`fees_add`
     - `src/pages/InternalNotesPage.tsx`：`internal_notes_add`
   - 只保留必要的 `Plus` import（若頁面沒有其他用到）。
   - 確保按鈕文字仍由 `useUiButtonLabel(...)` 決定、按鈕底色仍由 `useToolbarButtonUiProps(...)` 決定。

2. **Status label fix：Client Invoices list + detail 強制使用 `sl-invoice-*` ids**
   - `src/pages/ClientInvoicesPage.tsx`
     - 檢查並確保 `StatusBadge` 的狀態 id mapping 為：
       - `pending` -> `sl-invoice-pending`
       - `partial_collected` -> `sl-invoice-partial`
       - `collected` -> `sl-invoice-paid`
     - 檢查並確保 `statusOptionsList` 顯示文字同樣取自上述 `sl-invoice-*` options 的 `label`。
   - `src/pages/ClientInvoiceDetailPage.tsx`
     - 檢查並確保 `StatusBadge` 的狀態 id mapping 與顏色/文案取值同樣走 `sl-invoice-*`。

## 驗證步驟（請照做）
### Icon fix 驗證（四個 Add 按鈕）
1. 打開系統 `設定 → 工具列按鈕樣式`（或等同功能頁）。
2. 在圖示 panel 內，分別對以下按鈕 id 設定一個「明顯不同於 Plus」的 `lucide:*` 或自訂圖：
   - `translator_invoices_add`
   - `client_invoices_add`
   - `fees_add`
   - `internal_notes_add`
3. 前往各頁並確認頂部 Add 按鈕左側圖示已更新（按鈕文字不變）：
   - `src/pages/InvoicesPage.tsx`（新增請款單）
   - `src/pages/ClientInvoicesPage.tsx`（新增客戶請款單）
   - `src/pages/TranslatorFees.tsx`（新增費用）
   - `src/pages/InternalNotesPage.tsx`（新增內部註記）

### Status label fix 驗證（Client Invoices 清單 + 明細）
1. 進入 `設定 → 狀態標籤設定`，分別調整（至少文字或顏色其一）：
   - `sl-invoice-pending`、`sl-invoice-partial`、`sl-invoice-paid`
2. 打開 Client Invoices 清單頁（`ClientInvoicesPage`）：
   - 找到不同狀態的列，確認狀態 badge 顏色/文案跟你設定的 `sl-invoice-*` 對應一致。
   - 在 `FilterSortToolbar` 的 status 篩選條件中，確認選項顯示文案使用 `sl-invoice-*` 的 label。
3. 打開 Client Invoice 明細頁（`ClientInvoiceDetailPage`）：
   - 確認明細頁的狀態 badge 同樣使用 `sl-invoice-*` 顯示正確顏色/文案。

## Optional Further Checks（可選）
1. 切回預設圖示（移除 custom icon），確認四個 Add 按鈕回到 `defaultIconKey`（應為 `lucide:Plus`）。
2. 設定其中一個按鈕使用一個不在 `UiToolbarButtonIcon` 内建映射的 `lucide:*` key，確認 UI 不會爆炸、圖示可能為空但按鈕仍可操作（避免 runtime error）。
3. 若專案有 CI/lint，跑一次 `typecheck/lint` 以確認四個頁面的 import 修正不會留下 unused/import error。

## 請你確認
請回覆「確認」表示我要開始實作；或指出你想調整的點（例如：icon 解析格式、status badge 的映射規則、要不要把 fallback 行為也一併加上）。

