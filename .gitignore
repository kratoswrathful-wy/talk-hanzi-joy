# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist

# TypeScript 增量編譯快取（本機產物，勿進 Git；每次 tsc -b 都會變動造成噪音）
*.tsbuildinfo

# Local Storage export (migration download)
storage-backup/
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/tasks.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- 不要提交：本機密碼與暫存 ---
.env
.env.*
!.env.example
# .env.playwright.local 已被上面的 .env.* 涵蓋，此行僅為明示（見 architecture.mdc §9）
.env.playwright.local

# Supabase CLI 本機暫存（勿進 Git）
supabase/.temp/

# 遷移用個資 CSV（若不需要版本控制可保留此行）
scripts/profiles-migrate-users.csv

# 本機預覽／臨時 HTML（勿進 Git）
downloads/

# --- 客戶／正式案件檔勿進 repo（僅 tests/fixtures/ 可放去識別化最小樣本）---
# 規則見 .cursor/rules/architecture.mdc §11
*.sdlxliff
*.mqxliff
*.xlf
*.xliff
*.tmx
*.xlsx
!tests/fixtures/
!tests/fixtures/**

.vercel

# Playwright
playwright/.auth/
test-results/
playwright-report/
blob-report/