# CAT 第三波主記錄（摘要，可版控）

**狀態標記（2026-04-25）**：第三波已完成並由使用者驗收通過。

關聯主計畫：`cat_工具綜合改版_42ac9451.plan.md` 第 4～5 節（QA 範圍鎖、執行鎖、摘要、確認後清單、排序、譯文不一致等）。  
可版控鏡像：[`docs/mirror/cat_工具綜合改版_42ac9451.plan.md`](mirror/cat_工具綜合改版_42ac9451.plan.md)（與本機 `%USERPROFILE%\.cursor\plans\` 同名檔同步維護）。

**收斂說明**：原輔助計畫 `cat_第三波_qa_全套_eb571303.plan.md`（第三波執行／驗收節奏、總範圍、分階段說明、風險與迭代備註）已併入本檔下列各節；該檔已自本機 `.cursor/plans` **移除**，避免與本檔雙軌。

---

## 一、第三波完成清單（依階段／批次）

### 總範圍（原細計畫「全納入」清單，已全部落地並驗收）

- QA 結果表排序（欄位排序、`aria-sort`、略過列顯示規則與主計畫 §5 一致）。
- 「目前篩選結果」範圍鎖（整條篩選工具列鎖定、`title`／tooltip 定稿文案、`qaScopeLocksFilterUi`）。
- QA 執行中鎖定（紅框設定區＋「開始 QA」；「隱藏已忽略」執行中不鎖）；完成後解除並重設句段範圍（含「目前篩選結果」勾選）。
- 執行摘要（「現有 QA 執行摘要」）：時間、檢查項目、範圍、當時篩選條件（一行一條、群組攤開）；**保留至使用者下次按「開始 QA」**（主計畫 §4）。
- 確認後 QA 清單增量更新：刪除相關列後重跑 Tag／術語／數字；**不重跑 AI 錯字**。
- **譯文不一致**：確認後依**與整檔 `runQaChecks` 相同語意**之同原文（trim）鍵，於全檔有譯文句段重算；增量路徑並以單次掃描 `Map` 優化（`b33e471`）。原細計畫「僅區域、避免整檔」之草案語，已收斂為**語意對齊整檔**之定案。

### 第 2.5 階段（2026-04-25）

插隊於階段 2 與階段 3 之間；對齊主計畫 §2／§4 迭代。

| 項目 | 說明 | 參考 commit |
|------|------|----------------|
| 進階「使用中」定義與鎖定 | TM%／句段狀態／列範圍任一參與篩選即鎖；與面板摺疊無關；Ctrl+F 與模式鈕同一判斷 | `a66a1e9` |
| QA 摘要與 modal | 「現有 QA 執行摘要」、當時篩選一行一條、群組攤開、自訂確認視窗、括號外無句點 | `a66a1e9` |
| 模式切換改為生效標籤 | 使用中時以「進階篩選／生效中🔒」取代兩顆切換鈕；QA 確認主句與補充分兩行 | `59b7d91` |
| 生效標籤反灰 | 標籤改淺灰底、邊框、中灰字（反灰顯示） | `f33646b` |

### 階段 3（2026-04-25）

確認後 **Tag／術語／數字／譯文不一致** 增量重算；掛鉤於 **Ctrl+Enter**、**狀態圖示確認**、**多選批次確認** 之 `enqueueConfirmSideEffects` 尾端（TM／重複傳播完成後）。**參考 commit**：`bd4ace3`。

### 階段 3 補充（2026-04-24）

確認後 **樂觀第一段**：`isConfirming` 結束後立刻對剛確認句 id 呼叫 `_qaIncrementalRefreshAfterConfirm`，尾端仍執行第二段收斂。**使用者驗收通過**。**參考 commit**：`a341fda`。

### 增量路徑效能（階段 3／譯文不一致收尾子項）

`_qaIncrementalRefreshAfterConfirm` 內以單次走訪建立「trim 原文 → 有譯文句段」對照。**參考 commit**：`b33e471`（使用者已併入第三波驗收通過）。

---

## 二、測試與驗收記錄

- **自動化**：各階段交付時依需要執行 `npm run test:cat-sf`、`npm test`；結案前已滿足當時約定。
- **手動 smoke**（原細計畫節奏，已全部納入驗收）：QA 範圍鎖定／解鎖、執行中紅框與「開始 QA」狀態、執行摘要保留至下次「開始 QA」、確認後結果表更新、譯文不一致與同原文多句、樂觀第一段體感。
- **結論**：使用者確認**第三波所有曾要求驗收之項目均已通過**（最後收斂日：2026-04-25）。
- **同步**：`cat-tool` 變更均經 `npm run sync:cat` 一併提交 `public/cat`。

---

## 三、衍生文件盤點與收斂

| 文件 | 處置 |
|------|------|
| `%USERPROFILE%\.cursor\plans\cat_第三波_qa_全套_eb571303.plan.md` | 內容已併入本檔第一～二節與總範圍說明；**已刪除** |
| `docs/CAT第三波主記錄.md`（本檔） | **保留**為第三波唯一版控主記錄 |
| `docs/mirror/cat_工具綜合改版_42ac9451.plan.md` | **保留**；結案時已更新 frontmatter 中 `qa-incremental-sort` 為 `completed` 並於正文加狀態列 |

---

## 四、結案判定

1. **第三波工作是否全部結束？** **是。**
2. **主計畫中本波對應範圍（QA §4～§5 及確認後清單主線）是否已完成？** **是。** 後續 **第四波**（主計畫 §3 樂觀鎖／協作誤報等）屬另一波次，不計入本結案。

---

## 附錄：原細計畫之驗收節奏與風險（存檔）

以下為原 `eb571303` 輔助計畫條目，供日後查考；執行上已由上列各節吸收。

- **驗收節奏**：每階段 `npm run test:cat-sf`、`npm test`；手動 smoke 同上；交付為中文推送＋階段驗收。
- **風險控制**：先鎖定與摘要，再增量重算，最後譯文不一致與樂觀／效能收斂，降低狀態組合一次爆炸之風險。
