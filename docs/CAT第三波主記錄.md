# CAT 第三波主記錄（摘要，可版控）

完整規劃與關聯主計畫仍以 Cursor 計畫檔為準：  
`%USERPROFILE%\.cursor\plans\cat_第三波_qa_全套_eb571303.plan.md`（若檔名有調整請以該目錄為準）。

關聯主計畫：`cat_工具綜合改版_42ac9451.plan.md` 第 4 節等 QA 相關章節。  
可版控鏡像：[`docs/mirror/cat_工具綜合改版_42ac9451.plan.md`](mirror/cat_工具綜合改版_42ac9451.plan.md)（與本機 `%USERPROFILE%\.cursor\plans\` 同名檔同步維護）。

---

## 第 2.5 階段完成與驗收（2026-04-25）

插隊於階段 2 與階段 3 之間。使用者其餘驗收項已確認無誤。

| 項目 | 說明 | 參考 commit |
|------|------|----------------|
| 進階「使用中」定義與鎖定 | TM%／句段狀態／列範圍任一參與篩選即鎖；與面板摺疊無關；Ctrl+F 與模式鈕同一判斷 | `a66a1e9` |
| QA 摘要與 modal | 「現有 QA 執行摘要」、當時篩選一行一條、群組攤開、自訂確認視窗、括號外無句點 | `a66a1e9` |
| 模式切換改為生效標籤 | 使用中時以「進階篩選／生效中🔒」取代兩顆切換鈕；QA 確認主句與補充分兩行 | `59b7d91` |
| 生效標籤反灰 | 標籤改淺灰底、邊框、中灰字（反灰顯示） | `f33646b` |

**階段 3（2026-04-25）**：確認後 QA 一般規則增量重算已實作並推送。

- **行為**：在曾執行過 QA（`_qaResults` 非空）的前提下，於 **Ctrl+Enter 確認**、**狀態圖示確認**、**多選 Ctrl+Enter 批次確認** 且 **TM／重複傳播完成後**（`enqueueConfirmSideEffects` 尾端），依目前 QA 面板勾選之 **Tag／術語／數字／譯文不一致** 重算受影響句段；**錯字（AI）列保留不重跑**。
- **同原文**：凡觸及句段之原文鍵，會清除該原文下所有「譯文不一致」列後再依**全檔**同原文句段重算一致性（與整檔 QA 語意一致）。
- **參考 commit**：`bd4ace3`

**階段 3 補充（2026-04-24）**：確認後 QA **樂觀第一段**（使用者驗收通過）。

- **動機**：階段 3 的增量重算在 `enqueueConfirmSideEffects` 尾端執行，TM／重複傳播完成前 QA 列約有 1–2 秒才更新。
- **行為**：在 **Ctrl+Enter**、**狀態圖示確認**、**多選批次確認** 路徑上，於 `isConfirming` 結束後**立刻**對剛確認的句段 id 呼叫 `_qaIncrementalRefreshAfterConfirm`（Tag／術語／數字與觸及原文之譯文不一致先更新）；傳播與 TM 完成後**仍執行**尾端同一函式做第二段收斂（行為與僅階段 3 時一致）。
- **參考 commit**：`a341fda`

---

## 階段 4（進行中）

**目標**：將「譯文不一致」在**確認後增量路徑**的演算收斂為較省的全檔單次掃描（維持與整檔 QA 相同語意），必要時再評估更細的邊界或結案整理。

**已啟動子項**

| 子項 | 說明 | 狀態 |
|------|------|------|
| 增量路徑同原文索引 | `_qaIncrementalRefreshAfterConfirm` 內以單次走訪建立「trim 原文 → 有譯文句段」對照，取代每個 touched source 重複 `filter` 全檔 | 已完成 `b33e471` |

（計畫檔仍以本檔開頭所列 Cursor plan 為準；此處為可版控摘要。）
